// Generated by CoffeeScript 2.5.1
(function() {
  'use strict';
  var $, $async, CND, PD, XE, alert, badge, debug, defer, help, info, jr, rpr, urge, warn, whisper;

  //###########################################################################################################
  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'PIPEDREAMS/EXPERIMENTS/XEMITTER';

  debug = CND.get_logger('debug', badge);

  alert = CND.get_logger('alert', badge);

  whisper = CND.get_logger('whisper', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  info = CND.get_logger('info', badge);

  //...........................................................................................................
  PD = require('../..');

  jr = JSON.stringify;

  defer = setImmediate;

  ({$, $async} = PD);

  XE = PD.XE.new_scope();

  (async() => {    //###########################################################################################################
    var add, multiply, unsubscribe_add, unsubscribe_multiply;
    //---------------------------------------------------------------------------------------------------------
    add = function(d) {
      return XE.emit(PD.new_datom('^result', d.a + d.b));
    };
    multiply = function(d) {
      return XE.emit(PD.new_datom('^result', d.a * d.b));
    };
    //---------------------------------------------------------------------------------------------------------
    // XE.listen_to_all ( key, d ) -> info 'µ1', "#{key}: #{rpr d}"
    XE.listen_to('^compute', function(d) {
      return help('µ1', `^compute: ${rpr(d)}`);
    });
    XE.listen_to('^result', function(d) {
      return warn('µ1', `^result: ${rpr(d)}`);
    });
    //.........................................................................................................
    whisper("subscribe add()");
    unsubscribe_add = XE.listen_to('^compute', add);
    await XE.emit(PD.new_datom('^compute', {
      a: 42,
      b: 108
    }));
    //.........................................................................................................
    whisper("unsubscribe add()");
    unsubscribe_add();
    await XE.emit(PD.new_datom('^compute', {
      a: 42,
      b: 108
    }));
    //.........................................................................................................
    whisper("subscribe multiply()");
    unsubscribe_multiply = XE.listen_to('^compute', multiply);
    await XE.emit(PD.new_datom('^compute', {
      a: 42,
      b: 108
    }));
    //.........................................................................................................
    whisper("unsubscribe multiply()");
    unsubscribe_multiply();
    return (await XE.emit(PD.new_datom('^compute', {
      a: 42,
      b: 108
    })));
  })();

}).call(this);

//# sourceMappingURL=demo-xemitter-unsubscribe.js.map
